# Stub: sam_my.py imports `CNN` from this module but never instantiates it
# in Sam_my.__init__/forward. Never present in this repo's git history
# (checked). Stub only so `import sam_my` doesn't crash on the dead import.
class CNN:
    pass
