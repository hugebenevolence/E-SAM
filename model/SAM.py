# Stub: train.py imports `SAM` from this module but never instantiates it
# (the actual model comes from sam_model_registry['vit_b'] -> Sam_my).
# The real model/SAM.py was never present in this repo's git history
# (checked: no commit ever added it). This stub exists only so
# `import train` doesn't crash on the dead import.
class SAM:
    pass
