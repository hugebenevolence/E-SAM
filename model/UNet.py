# Stub: train.py imports `U_Net` from this module but never instantiates it
# for the MMWHS/Sam_my training path. Never present in this repo's git
# history (checked). Stub only so `import train` doesn't crash.
class U_Net:
    pass
