# Stub: sam_my.py does `from model.ExpertChoiceTokenConvLoRAMoE import *`
# but never references any symbol from it (the MoE class Sam_my actually
# uses, ExpertChoiceTokenSparseMoE, comes from `model.MoE`, imported
# separately). Never present in this repo's git history (checked). Empty
# stub only so `import sam_my` doesn't crash on the dead import.
