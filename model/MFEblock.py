# Stub: image_encoder.py does `from model.MFEblock import *` but never
# references any symbol from it elsewhere in that file. Never present in
# this repo's git history (checked). Empty stub only so the import chain
# doesn't crash on this dead import.
